# The server only serves the built frontend now — the conversion runs in the
# browser (frontend/src/engine/). numpy, pandas, scikit-learn, Pillow and
# openpyxl are gone with the API routes that needed them.
#
# The DMC chart is generated into the bundle by scripts/export-dmc.py, which
# needs openpyxl — but that runs on a developer machine, never in production.

fastapi==0.116.1
uvicorn[standard]==0.23.2
